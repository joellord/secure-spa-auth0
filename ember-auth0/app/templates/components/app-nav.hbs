<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand" href="#">Secure your SPA</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item active" data-route="#">
        {{#link-to 'home' class="nav-link"}}Home{{/link-to}}
      </li>
      <li class="nav-item" data-route="#secret">
        {{#link-to 'secret' class="nav-link"}}Secret Area{{/link-to}}
      </li>
    </ul>
    <form class="form-inline">
      {{#if loggedIn}}
        <button {{ action "logout" }} class="btn btn-outline-danger" type="button" id="logoutBtn">Logout</button>
      {{else}}
        <button {{ action "login" }} class="btn btn-outline-success my-2 my-sm-0" type="button" id="loginBtn">Login</button>&nbsp;
      {{/if}}

    </form>
  </div>
</nav>